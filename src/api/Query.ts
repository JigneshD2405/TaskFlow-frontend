const Query = {
  // Auth
  signIn: "/sign-in",
  register: "/register",
  refreshToken: "/refresh-token",
  signOut: "/sign-out",
  auth: "/auth",

  boards: "/boards",

  columns: "/columns",

  cards: "/cards",

  users: "/users",
};

export default Query;
